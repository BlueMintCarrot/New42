Here we had to create a file and write a command line inside that will list all files in a directory, not showing hidden files (the ones starting with '.' and '..'. 

ls -A

'ls' is the command to list evertyhing.
-A is the flag to ignore the hidden files.
